<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>AppPhx · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <div class="container">

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= link "Home", to: Routes.page_path(@conn, :index) %> |
        <%= link "List users", to: Routes.user_path(@conn, :index) %> |
        <%= if signed_in?(@conn) do %>
          <%= link "Sign out", to: Routes.session_path(@conn, :delete), method: :delete, data: [confirm: "Are you sure?"] %>
        <% else %>
          <%= link "Sign In", to: Routes.session_path(@conn, :new) %>
          |
          <%= link "Sign up", to: Routes.user_path(@conn, :new) %>
        <% end %>
        <%= @inner_content %>
      </main>

    </div> <!-- /container -->
  </body>
</html>
